type Query {
    loadMoreOrder(offset: Int, number: Int): Pagination 
}

type Mutation {
    updateOrder(uid: String! , input: orderInput): Order
    createOrder(input: orderInput): Order
    deleteOrder(uid: String!): Order
}


type Pagination {
    offset: Int!
    number: Int!
}