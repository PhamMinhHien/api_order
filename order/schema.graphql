type Query {
    searchByUid(id: String!) : Order 
    loadMoreOrder(number: Int!, offset: Int!) : [Order]
    listOrder(number: Int!, page: Int!, filter: String) : ListOrder
}

type Order {
    uid: String!
    order_id: String
}

type ListOrder {
    totalCount: Int!
    data: [Order]!
}